# Agent setup retrospective — 2026-07

## 2026-07-12 cutover

- Replaced seven Home Manager-owned skill directory symlinks with byte-matching
  mutable directory copies.
- Verified each migrated name through Codex's model-visible prompt-input loader.
- Retained runtime dependencies in Home Manager and removed the skill renderer.
- Added this recovery ledger and the weekly maintenance policy.
- Generalized bootstrap and review coverage across HM-installed Codex, Hermes,
  and Antigravity, with a single scheduler owner.
- Replaced the machine-local `safe-cli` input with its locked GitHub source.
- Baseline counts before cutover: 17 skills under `~/.agents/skills`, 7 under
  `~/.codex/skills`, and 91 `SKILL.md` files in concrete plugin cache versions.

No skills were culled during the baseline migration. Upstream renaming and
deduplication are deferred to a later weekly review so the ownership cutover
does not also change behavior.
